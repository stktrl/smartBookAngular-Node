import { MemberfilterPipe } from './memberfilter.pipe';

describe('MemberfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
