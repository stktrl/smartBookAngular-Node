import { RentbookfilterPipe } from './rentbookfilter.pipe';

describe('RentbookfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RentbookfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
